git clone <link>
git status
git checkout <branch>

dir (show files in directory)
cd <folder> (move to folder
cd .. (move one folder hirarchy down)

committing:
git status
git add . (add all files to be checked out)
git commit -m "<comment>" (commit and comment)
git push


refreshing repo:
git pull


stating browser sync (watch all files in that and lower directory)
browser-sync start --server --files "**/*"
 